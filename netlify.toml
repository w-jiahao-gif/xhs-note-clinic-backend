[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 所有路径（含 /health、/api/...）都交给同一个函数处理
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
